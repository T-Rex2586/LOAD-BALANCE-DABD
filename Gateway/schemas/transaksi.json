{
  "type": "object",
  "properties": {
    "kode_transaksi": { "type": "string", "minLength": 1, "maxLength": 30 },
    "nama_pembeli": { "type": "string", "maxLength": 50 },
    "nomor_meja": { "type": "string", "maxLength": 10 },
    "total_bayar": { "type": "number", "minimum": 0 },
    "metode_bayar": { "type": "string", "enum": ["tunai", "qris", "debit"] },
    "status_bayar": { "type": "string", "enum": ["lunas", "batal"] }
  },
  "required": ["kode_transaksi"],
  "additionalProperties": false
}
